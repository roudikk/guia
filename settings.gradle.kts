pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "compose-navigator"

include(":sample")
include(":compose-navigator")