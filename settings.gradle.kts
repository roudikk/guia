pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "compose-navigator"

include(":app")
include(":compose-navigator")
